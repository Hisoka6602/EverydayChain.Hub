# PR-01/PR-02 与 PR-04/PR-05 分表回归与迁移校验清单（R1）

## 校验批次信息
- 记录日期：`2026-04-17`（本地时间）
- 关联阶段：PR-01、PR-02、PR-04、PR-05
- 归档目录：`docs/联调证据/PR12-20260416-R1/`

## 一、分表回归检查
| 检查项 | 结论 | 证据 |
|---|---|---|
| `business_tasks` 逻辑表分片命名与写入路径一致 | 通过 | `BusinessTaskRepository` 与分片解析链路实现已存在 |
| 扫描链路字段更新后可进入分片写入 | 通过 | `TaskExecutionServiceTests`、`BusinessTaskMaterializerTests` 历史基线通过 |
| 回写链路字段更新与本地状态闭环不破坏分片查询 | 通过 | `WmsFeedbackService`、`FeedbackCompensationService` 现有实现与回归测试通过 |

## 二、迁移执行检查
| 检查项 | 结论 | 证据 |
|---|---|---|
| 业务模型补齐迁移存在 | 通过 | `Infrastructure/Migrations/20260417043253_AddBusinessTaskClosureFields.cs` |
| 构建后迁移模型快照可编译 | 通过 | `dotnet build EverydayChain.Hub.sln` |
| 自动迁移启动链路保持可用 | 通过 | `AutoMigrationService` 仍在启动链路注册与执行 |

## 三、上线前 SQL 校验清单
| 检查项 | 状态 | 说明 |
|---|:---:|---|
| 表结构变更 SQL 语义校验（字段、索引、默认值） | [x] | 已完成迁移脚本与实体映射一致性复核 |
| 分表模板 SQL 校验（逻辑表到物理表命名） | [x] | 已完成分片命名规则与逻辑表定义复核 |
| 预发布库执行迁移并回滚演练 | [ ] | 需在目标环境执行，当前仓库仅完成代码侧清单补录 |

## 四、回归命令记录
1. `dotnet build EverydayChain.Hub.sln`（通过）
2. `dotnet test EverydayChain.Hub.sln --no-build`（通过）
