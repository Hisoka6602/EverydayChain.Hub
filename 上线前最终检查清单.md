# 上线前最终检查清单

## 0. 本次上线口径（先决结论）
- 本次采用方案 A：当前版本可直接上线，但 WMS 回写保持关闭。
- 生产配置要求：`WmsFeedback.Enabled=false`。
- 判定规则：
  - 方案 A：可上线（WMS 回写不作为阻塞项）。
  - 方案 B：仅当 WMS 启用门禁全部通过后可上线，否则不可上线。

## 1. 基础环境检查（开发/运维）
- [ ] Web 监听地址确认：`WebEndpoint.Url` 与发布环境地址一致。
- [ ] Swagger 路径确认：`Swagger.Path` 与网关/反向代理路由一致。
- [ ] SQL Server 连接可用：启动后可完成健康查询与读写验证。
- [ ] Oracle 连接可用：联调链路连接测试通过（即使本次默认关闭回写也需保留可用性验证记录）。

## 2. 数据库与迁移检查（开发/运维/测试）
- [ ] 新基线迁移已在目标环境应用完成。
- [ ] 自动迁移启动日志正常，无阻塞异常。
- [ ] 自动分表预建执行正常，无建表失败日志。
- [ ] 分表模板结构与索引检查通过（业务表与日志表均覆盖）。

## 3. API 检查（测试/业务）
- [ ] 现有分拣机接口兼容性通过（路由、协议、字段语义无破坏性变更）。
- [ ] 查询 API 正常（总看板、码头看板、业务任务/异常件/回流）。
- [ ] 报表导出正常（CSV 下载与编码格式正确）。
- [ ] Swagger 页面与 JSON 文档可访问、字段说明完整。

## 4. 文档检查（开发/测试/业务）
- [ ] `前端对接文档.md` 已更新到当前实现，并附“一致性校验结果”。
- [ ] `WMS回写联调基线.md` 已更新到最终上线口径。
- [ ] `Swagger注释全量盘点清单.md` 已完成逐文件盘点并留痕。

## 5. WMS 回写上线门禁（发布审批）
### 情况 A：本次上线不启用 WMS 回写（本次采用）
- [ ] `WmsFeedback.Enabled=false` 保持不变。
- [ ] 系统可上线。
- [ ] WMS 回写不作为本次上线阻塞项。

### 情况 B：本次上线必须启用 WMS 回写
以下条件必须全部满足，否则本次不可上线：
- [ ] Oracle 写权限已签核。
- [ ] 目标表触发器/行级规则副作用验证通过。
- [ ] 发布窗口与回滚窗口审批完成。
- [ ] 联调五项验证全部通过。

## 6. 发布后观察项（运维/值班）
- [ ] WMS 回写状态（本次应保持关闭）。
- [ ] 补偿任务状态（失败量、重试量、堆积量）。
- [ ] 看板查询响应时延与失败率。
- [ ] 报表导出响应时延与失败率。
- [ ] 分片表创建情况（是否按月正常预建）。
- [ ] 错误日志关键字（连接失败、迁移失败、分表失败、回写失败）持续观察。

## 7. 最终发布判定
- 可上线条件（本次）：满足“情况 A”全部检查项即可发布。
- 不可上线条件：若要求启用 WMS 回写但“情况 B”任一项未通过，则禁止发布。
